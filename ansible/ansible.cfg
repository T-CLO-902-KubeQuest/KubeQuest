# Allow access to VMs without asking for confirmation
# This is only a school example, do not use this in production
[defaults]
host_key_checking = False
interpreter_python = %(VIRTUAL_ENV)s/bin/python3
