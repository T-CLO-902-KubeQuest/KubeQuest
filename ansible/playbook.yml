---
- name: Prepare Linux nodes for Kubernetes
  hosts: aws_ec2
  gather_facts: true
  become: true
  roles:
    - common

- name: Install Kubernetes tooling on cluster nodes
  hosts: aws_ec2
  become: true
  gather_facts: true
  tags: kubeadm
  roles:
    - kubeadm
