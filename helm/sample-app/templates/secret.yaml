{{- if not .Values.app.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sample-app.fullname" . }}
  labels:
    {{- include "sample-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  APP_KEY: {{ required "app.key is required when app.existingSecret is unset" .Values.app.key | quote }}
  DB_PASSWORD: {{ required "db.password is required when app.existingSecret is unset" .Values.db.password | quote }}
{{- end }}
